spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.jdbc.time_zone: UTC
    show-sql: false
  flyway:
    enabled: true

logging:
  level:
    root: WARN
    no.sogn.plantdata: INFO
